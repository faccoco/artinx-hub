# develop分支更新日志

## 2022

### 10.04更新

- 重构了装甲板识别部分的代码，删除了没必要的判断
- 注意代码注释中灯条矩形、装甲板矩形长宽及角度的定义
- 将装甲板排序标准改为矩形倾角

### 10.06更新

- 修改装甲板识别部分代码部分注释
- 发现minAreaRect对轮廓拟合返回的旋转矩形形状很奇怪，无法拟合灯条形状
- 将fitEllipse()函数返回的旋转矩形作为拟合灯条的矩形
- 增加`tool_tutorials`文件的内容

### 10.14更新

- 去掉了HeadInfo结构体中yawspeed和pitchspeed变量
- 去掉了PostureData结构体中的角速度和角加速度等多余的变量
- 去掉fakeCarDetector这一层actor
- 修改了装甲板识别actor的处理结果的结构体，具体见DetectedArmor.hpp

### 10.23更新

- 将装甲板排序标准改回到原来根据装甲板到大小装甲板比率那个更接近的排序方式
- 将装甲板定位中，大小装甲板判别方式的比率设为了一个可调节参数

### 11.13更新

- 修改了transform.hpp中的部分接口
- 修改了模拟器中子弹击中判别的逻辑
- 增加了神经网络识别装甲板模块

### 11.15更新

- 增加了神经网络识别装甲板定位模块
- 在相机获取一张图片后记录下此时云台姿态(即机器人坐标到枪管坐标系的变换矩阵)，保证相机帧和云台姿态对应
- 预测后单独用一个predictedTarget保存数据

### 12.28 更新

- 增加神经网络识别装甲板感兴趣区域
- 修复神经网络识别装甲板快排bug

## 2023

### 1.12更新

- 相机驱动新增功能:
  - 自动白平衡
  - 全通道增益

### 2.24更新

- BlackBoard现在可以发送多个数据(多于一个时打包为tuple):
- HttpServer接受数据格式改为"CameraFrame,std::string_view",后者为在前端显示的标签名 eg: `sendAll(image_frame_atom_v, BlackBoard::instance().updateSync(mKey, std::move(res), std::string_view("VideoReplay")));`
- 加入雷达前端

### 2.25

- 修复了重定义问题，现在可以使用clang进行编译(由于cmake支持问题暂时无法使用lld进行链接)

### 3.25
- 增加前哨站预测击打功能
- 增加记录功率功能，加入了spdlog库
- 删除simulator中的fakeHead和fakeIMU

### 4.29
- 增加了雷达与裁判系统通讯的功能，目前只有雷达单向同步位置

### 4.30
- 增加反小陀螺算法守株待兔式击打方法
- 重构装甲板识别算法，装甲板、灯条倾角不由opencv旋转矩形定义获取


### 5.9
- 增加新的数字识别模型

### 5.13
- 增加整车运动估计actor
- 相机驱动中增加yaw角偏置
- 神经网络识别装甲板模块增加传统视觉提取角点，提升识别精度

### 6.24
- 以时间顺序(文件名)播放同一文件夹下的所有视频
