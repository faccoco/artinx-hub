global {
#     gForce = -9.8 # General
    gForce = -9.7949 # Nanjing(near to Changzhou)
#     gForce = -9.7833 # Guangzhou(near to Shenzhen)
    dragCoefficient = 0.0
    bullet42mm = false
}

server {
    type = "HttpServer"
}

camera {
    type = "DahengDriver"
    openMode = "Index"
    identifier = 1
    fps = 210.0
    fov = 30.0
    enableAutoWhiteBalance = true
    exposureTime = 0.001
    dy = -0.03 # TODO
    dx = -0.03 # TODO
    pitch = 1.0
    image_frame_atom = ["armor_detector","server","recorder"]
}

armor_detector {
    type = "ArmorDetector"
    globalScale = 1.0
    thresholdForBlue = [60,256,256]              # minBlue, maxGreen, maxRed
    thresholdForRed = [40,256,256]               # minRed, maxBlue,maxGreen
    bgrSubtractForBlue = 60                      # bgr subtract threshold for blue
    bgrSubtractForRed = 60                       # bgr subtract threshold for red
    minLightRectRatio = 0.15                     # width/height
    maxLightRectRatio = 0.6                      # width/height
    maxLightAngle = 60.0                         # angle(degree)
    maxAreaRatio = 2.0                           # ellipseArea/contourAre#
    minArmorRectRatio = 1.43                     # height/width
    maxArmorRectRatio = 6.67                     # height/width
    maxArmorAngle = 10.0                         # angle(degree)
    maxLightBaseAngle = 15.0                     # abs(lightAngle-armorAngle)(degree)
    maxParallelAngle = 10.0                      # abs(lightAngle1-lightAngle2)(degree)
    minLightHeightRatio = 0.6                    # lightHeight/armorHeight
    armor_detect_available_atom = ["armor_locator", "drawer"]
    image_frame_atom = ["server"]
}

drawer {
    type = "ArmorDetectorDrawer"
    image_frame_atom = ["server"]
}

# energy_detector {
#     type = "EnergyDetector"
#     smallPredictMode = 0
#     bigPredictMode = 2
#     armorMinArea = 900.0
#     armorMaxArea = 2000.0
#     armorMinWHRatio = 1
#     armorMaxWHRatio = 3
#     armorMinAreaRatio = 0.8
#     stripMinArea = 7500.0
#     stripMaxArea = 10000.0
#     stripMinWHRatio = 1
#     stripMaxWHRatio = 5
#     stripMaxAreaRatio = 0.7
#     noiseArea = 2500.0
#     predictAngle = 0.402768288
#     radius = 148
#     offsetX = 0.0
#     offsetY = 0.0
#     preFrames = 20
#     energy_detect_available_atom = ["strategy"]
#     image_frame_atom = ["server"]
# }

armor_locator {
    type = "ArmorLocator"
    ratioThreshold = 13
    detect_available_atom = ["strategy"]
}

strategy {
    type = "InfantryStrategy"
    set_target_atom = ["predict"]
}

predict{
    type = "ArmorPredictor"
    enablePredictor = false
    maxResidual = 1.0
    P = [0.05, 0.03, 0.1, 1.5, 1.5, 1.5]
    Q = [0.001, 0.001, 0.003, 0.001, 0.001, 0.003]
    R = [0.011206584, 0.0045113561, 0.0240242645488644]
    predict_success_atom = ["solver"]
}

solver {
    type = "AngleSolver"
    precision = 0.001
    delay = 0.20
    bulletSpeedDiscount = 1.0
    set_target_info_atom = ["serial"]
}

serial {
    type = "SerialPort"
    devPath = "/dev/ttyUSB0"
    baudRate = 921600
    # enableEnergyControl = true
    # energy_detector_control_atom = ["strategy","energy_detector"]
    update_head_atom = ["camera"]
    update_posture_atom = ["predict"]
}

recorder {
    type = "VideoRecorder"
    base = "record"
}
