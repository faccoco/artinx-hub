global {
    gForce = -9.7833 # Guangzhou(near to Shenzhen)
    dragCoefficient = 0.0
    bullet42mm = false
}

rosCamera {
    type = "RosCamera"
    imageTopic = "/image"
    cameraInfoTopic = "/camera_info"
    image_frame_atom = ["server"]
}

server {
  type = "HttpServer"
}