global {
  gForce = -9.8
  dragCoefficient = 0.5
  airDensity = 1.293
  bullet42mm = false
}
simulator {
  type = "Simulator"
  step = 0.001
  v0 = 15.0
  v0Std = 0.0
  shootInterval = 0.1

  maxTime = 12.0
  bulletCount = 100

  vibrationLinearRange = 0.0 
  vibrationAngleRange = 0.0
  spinningSpeed = 0.0

  standardDistance = 5.0
  sourceHeight = 0.15
  targetHeight = 1.166

  targetType = "Outpost"
  targetMotionType = "Spinning"
  sourceMotionType = "Static"

  expectedCount = 90

  printBulletInfo = true

  simulator_step_atom = ["detector","head","imu"]
}
detector {
  type = "FakeDetector"
  
  detect_available_atom = ["strategy"]
}
imu {
  type = "FakeIMU"
  
  update_posture_atom = ["predictor","outpostPredictor"]
}
head {
  type = "FakeHead"
  headHeightOffset = 0.2
  
  update_head_atom = ["simulator","detector"]
}

predictor {
  type = "ArmorPredictor"
  enablePredictor = false
  predict_success_atom = ["solver"]
}
outpostPredictor{
  type = "OutpostPredictor"
  enablePredictor = false
  outpost_predict_success_atom = ["solver"]
}
solver {
  type = "AngleSolver"
  precision = 0.005
  delay = 0.0
  set_target_info_atom = ["head","simulator"]
}
strategy {
  type = "SimpleStrategy"
  aimType = "Outpost"
  set_target_atom = ["predictor"]
  set_outpost_atom = ["outpostPredictor"]
}